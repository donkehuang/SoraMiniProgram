# 数据库权限配置 - 当前用户读写

## 问题
需要实现：
1. 已登录用户可以读写自己的数据
2. 未登录用户提示登录

## 解决方案

### 1. 微信云开发控制台配置

打开微信云开发控制台 → 数据库 → `videos` 集合 → 权限设置

**设置权限为自定义安全规则：**

```json
{
  "read": "auth.openid != null",
  "write": "auth.openid != null && doc._openid == auth.openid"
}
```

**权限说明：**
- `read: "auth.openid != null"` - 只有登录用户才能读取数据
- `write: "auth.openid != null && doc._openid == auth.openid"` - 只有登录用户才能写入，且只能操作自己的数据

### 2. 小程序代码修改

在保存视频前检查登录状态，未登录时提示用户登录。

**修改位置：** `miniprogram/pages/index/index.js`

在 `saveVideoToDatabase` 方法前添加登录检查。

## 配置步骤

### 步骤1: 微信云开发控制台配置
1. 打开微信开发者工具
2. 点击顶部菜单 "云开发" → "云开发控制台"
3. 选择 "数据库" 标签
4. 找到 `videos` 集合，点击进入
5. 点击 "权限设置" 标签
6. 选择 "自定义安全规则"
7. 粘贴上面的JSON配置
8. 点击 "保存" 按钮

### 步骤2: 小程序代码已自动更新
- 添加了登录状态检查
- 未登录时自动引导用户登录
- 登录后继续视频保存流程

## 测试验证

### 1. 测试未登录状态
- 清除小程序缓存
- 生成视频
- 应该提示"请先登录后再生成视频"
- 自动跳转到"我的"页面

### 2. 测试登录状态
- 在"我的"页面点击"微信登录"
- 授权登录
- 返回首页生成视频
- 视频应该正常保存到作品

### 3. 测试权限隔离
- 不同用户只能看到自己的作品
- 无法修改其他用户的数据

## 注意事项

1. **首次配置**：权限修改后立即生效，无需重启
2. **数据安全**：每条数据都会自动添加 `_openid` 字段标识创建者
3. **历史数据**：之前未登录时保存的数据可能没有 `_openid`，需要手动清理或迁移
4. **登录方式**：使用微信官方登录，无需额外配置

## 常见问题

### Q: 提示"database permission denied"
A: 检查数据库权限配置是否正确保存

### Q: 登录后仍然无法保存
A: 检查是否成功获取到 openid，查看控制台日志

### Q: 想要允许未登录用户读取数据
A: 修改 read 规则为 `"read": true`，但不建议这样做

## 安全优势

✅ 防止未授权访问  
✅ 数据隔离，用户只能访问自己的数据  
✅ 自动权限校验，无需后端代码  
✅ 符合微信小程序规范  

---
配置完成后，所有数据读写都需要登录，保护用户隐私和数据安全。
