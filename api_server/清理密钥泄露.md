# 🚨 API密钥泄露应急处理

## 已发现的泄露点

1. ❌ `test_openai_connection.py` - 硬编码了完整API密钥
2. ❌ `fix_env_final.sh` - 包含完整API密钥
3. ⚠️ 可能已提交到Git历史

## 立即执行的清理步骤

### 1. 撤销OpenAI API密钥（最重要！）

1. 访问：https://platform.openai.com/api-keys
2. 找到泄露的密钥（结尾是 `ueIegA`）
3. 点击右侧的 "Revoke" 或 "Delete" 删除
4. 创建新的API密钥

### 2. 清理Git历史中的密钥

```bash
cd /home/admin/SoraMiniProgram

# 方法1：如果还没push到远程，可以修改最近的提交
git log --oneline -5

# 方法2：使用git filter-repo清理历史（推荐）
# 安装git-filter-repo
pip3 install git-filter-repo

# 清理包含密钥的文件
git filter-repo --path api_server/test_openai_connection.py --invert-paths
git filter-repo --path api_server/fix_env_final.sh --invert-paths

# 方法3：如果仓库是私有的且只有你一个人，可以强制推送
git push origin master --force
```

### 3. 检查GitHub仓库

如果代码已push到GitHub：

1. 访问你的GitHub仓库设置
2. 检查仓库是否是**私有的**（Private）
3. 如果是公开的（Public），立即改为私有
4. 或者删除整个仓库，清理后重新创建

### 4. 本地清理已完成

✅ 已删除 `test_openai_connection.py`
✅ 已删除 `fix_env_final.sh`
✅ 已更新 `.gitignore` 防止未来泄露

### 5. 更新.gitignore并提交

```bash
git add .gitignore
git add -u  # 记录删除的文件
git commit -m "security: 移除包含API密钥的文件并更新.gitignore"
```

### 6. 使用新密钥配置服务器

```bash
# 在服务器上
cd /home/admin/SoraMiniProgram/api_server

# 使用新密钥创建.env
nano .env

# 输入（使用新的API密钥）：
OPENAI_API_KEY=sk-proj-你的新密钥
OPENAI_BASE_URL=https://api.openai.com/v1

# 保存并重启服务
chmod 600 .env
sudo systemctl restart sora-api
```

## 防止未来泄露的最佳实践

### ✅ 正确做法

```python
# 使用环境变量
from dotenv import load_dotenv
import os

load_dotenv()
api_key = os.getenv('OPENAI_API_KEY')
```

### ❌ 错误做法

```python
# 永远不要硬编码API密钥！
api_key = 'sk-proj-xxxxx'  # ❌ 危险！
```

### .gitignore 必须包含

```gitignore
.env
.env.*
*.key
*_env_*.sh
test_openai*.py
```

### 代码审查清单

提交前检查：
- [ ] 没有硬编码API密钥
- [ ] 没有包含密钥的测试文件
- [ ] 没有包含密钥的脚本文件
- [ ] .env文件已被.gitignore忽略
- [ ] 文档中的密钥示例已脱敏

## OpenAI密钥泄露检测

OpenAI会自动扫描GitHub等公开平台：
- 如果检测到密钥泄露，会自动撤销
- 你会收到邮件通知
- 立即创建新密钥并更新配置

## 紧急联系

如果密钥已被滥用：
1. 检查OpenAI账单：https://platform.openai.com/usage
2. 联系OpenAI支持：https://help.openai.com
3. 考虑启用API密钥的使用限制（rate limits）

---

**当前状态：**
- ✅ 本地文件已清理
- ⏳ 需要：撤销旧密钥
- ⏳ 需要：创建新密钥
- ⏳ 需要：更新服务器配置
- ⏳ 需要：检查GitHub仓库状态
